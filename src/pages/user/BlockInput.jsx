import React from "react";


function BlockInput ({
    value
})
{


    chkEvent(e) => {
        set
    }

    return(
        <TextField
         fullWidth
         onChange={(e) => chkEvent(e.currentTarget.value)}
         />
    )
}

export default BlockInput;